/**
 * The minimum width the floating content can have when shown on the left side of the screen
 * When the content is too small, it will behave as a "popover" content
 */
export const MIN_FLOATING_CONTENT_WIDTH = 200;

/**
 * The base offset of the floating content when shown as a "popover"
 */
export const FLOATING_CONTENT_OFFSET = 32;

/**
 * The delay in milliseconds for the auto-close timer of the floating content in tooltip mode
 */
export const AUTO_CLOSE_TOOLTIP_DELAY = 3000;
